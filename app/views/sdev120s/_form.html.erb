<%= form_with(model: sdev120, local: true) do |form| %>
  <% if sdev120.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sdev120.errors.count, "error") %> prohibited this sdev120 from being saved:</h2>

      <ul>
      <% sdev120.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
